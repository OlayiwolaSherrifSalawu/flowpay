import 'package:flutter/material.dart';

class FlowPayColors {
  // Backgrounds & Surfaces (Linear & BMONI dark fintech aesthetic)
  static const background = Color(0xFF0B0F17);
  static const surface = Color(0xFF131926);
  static const surfaceElevated = Color(0xFF1B2234);
  static const surfaceSubtle = Color(0xFF171E2D);

  // Borders & Dividers
  static const border = Color(0xFF222B3F);
  static const borderLight = Color(0xFF2E3B56);

  // Brand Accents
  static const primary = Color(0xFF6366F1);       // Electric Indigo
  static const primaryLight = Color(0xFF818CF8);
  static const primaryDark = Color(0xFF4F46E5);

  static const accent = Color(0xFF10B981);        // Emerald Green (Settlement / Success)
  static const accentLight = Color(0xFF34D399);

  static const warning = Color(0xFFF59E0B);       // Amber
  static const error = Color(0xFFEF4444);         // Coral Red

  // Text Hierarchy
  static const textPrimary = Color(0xFFF8FAFC);
  static const textSecondary = Color(0xFF94A3B8);
  static const textTertiary = Color(0xFF64748B);
  static const textMuted = Color(0xFF475569);

  // Currency Badge Colors
  static const usdBadge = Color(0xFF1E293B);
  static const ngnBadge = Color(0xFF064E3B);
  static const mxnBadge = Color(0xFF701A75);
}
